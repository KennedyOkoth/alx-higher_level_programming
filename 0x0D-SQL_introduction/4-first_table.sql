-- Creates  a table called "first_table" in the current database with these requirements:
-- - id INT
-- - name VARCHAR(256)

CREATE TABLE IF NOT EXISTS first_table (
    id INT,
    name VARCHAR(256)
);
